import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.emsc.app',
  appName: 'ems_capacitor',
  webDir: '/www',
  bundledWebRuntime: false
};

export default config;
